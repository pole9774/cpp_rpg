compile into main.exe:
    g++ main.cpp -o main -std=c++20

compile into main.exe (multiple files):
    g++ main.cpp shape.cpp rectangle.cpp circle.cpp -o main -std=c++20

run main.exe:
    .\main.exe
